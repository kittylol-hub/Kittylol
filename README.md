# Kittylol

A tiny daily dose of cat-powered joy.

## Run with Alloy

The checked-in Alloy Compose stack runs the site at `http://localhost:3000`:

```sh
docker compose -f docker-compose.alloy.yaml up -d
```

The service uses host networking and has no host-side dependencies beyond Docker.
